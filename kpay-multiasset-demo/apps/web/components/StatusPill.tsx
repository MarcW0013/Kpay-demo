export function StatusPill({ status }: { status: "PENDING" | "SEEN" | "CONFIRMED" | "EXPIRED" }) { const styles: Record<typeof status,string> = { PENDING:"bg-neutral-100 text-neutral-700", SEEN:"bg-amber-100 text-amber-800", CONFIRMED:"bg-emerald-100 text-emerald-800", EXPIRED:"bg-rose-100 text-rose-800" }; return <span className={`inline-flex items-center rounded-full px-2.5 py-1 text-xs font-medium ${styles[status]}`}>{status}</span>; }
